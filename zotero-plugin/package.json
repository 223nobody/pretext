{
  "name": "zotero-pretext-reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && node scripts/build.mjs",
    "lint": "tsc --noEmit -p tsconfig.json",
    "verify": "node scripts/verify-package.mjs"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^6.0.3"
  }
}
